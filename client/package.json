{
  "name": "ptz-camera-client",
  "version": "1.0.0",
  "private": true,
  "description": "Browser/PWA control panel UI for the PTZ camera server. No build step — plain HTML/CSS/JS served as-is.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node serve.js",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
